# 第一阶段：轻量依赖（约 150 MB）—— 用于验证 Python 3.13 兼容性
# 安装：pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/

numpy
scipy
soundfile
librosa
pretty_midi
mido
onnxruntime
fastapi
uvicorn[standard]
python-multipart
pyyaml
tqdm
jinja2

# 第二阶段：重依赖（约 3 GB），用于四轨音源分离
# 确认第一阶段无误后再安装：
#   pip install -r requirements-heavy.txt -i https://mirrors.aliyun.com/pypi/simple/
# 内容：torch torchaudio demucs

# 说明：
# resampy 已从本清单移除。它依赖 numba，而 numba 对新版 numpy 的
# 跟进存在滞后，在 Python 3.13 + numpy 2.x 组合下失败概率高。
# librosa 的 resample 功能在当前版本已可用 soxr 后端，无需 resampy。
